#!/usr/bin/env bash
# Local gate matching AGENTS.md §4. Enable with:
#   git config core.hooksPath .githooks
set -euo pipefail
cd "$(git rev-parse --show-toplevel)"
python3 -m unittest discover -s tests -q
python3 agents/validate.py
make site
